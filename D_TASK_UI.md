# 陆空协同 D 题地面站

运行 `python3 land_air_app.py` 可启动第一、二问的只读显示界面。它复用
FleetBus 半双工主站，交替轮询 `DRONE (0x10)` 与 `CAR (0x20)`，在 400 x
500 cm FIELD 坐标中绘制两端的实时位置和历史轨迹。

这个入口不会发送起飞、移动、抛投、降落或启动任务命令；它只发送 FleetBus
`POLL` 请求。因此启动按钮仍应由小车端的一键流程处理。

端点应将自身的位置以 FleetBus `REPORT` 上报，并把任务阶段写入
`operation_state`。为使文字状态一致，D 题约定见
`components/d_task_status.py`；未知值会原样显示，便于后续联调。当前配置中的
FIELD 原点为左下角 H 点 `(0, 0)`；A、B、D 的准确位置应在赛场测量后写入
`fleet_config.json` 的 `field.markers`，不得从示意图猜测。

该入口默认使用独立的 `d_task_fleet_config.json`，因此不会改变既有
`fleet_app.py` 的灾情任务配置。它已按小车工程当前固定轨迹写入 A `(150, 200)`、
B `(150, 350)`、C `(300, 350)`、D `(300, 200)`；小车变换的原点暂按其当前默认
“雷达中心在 A 后 20 cm”填写为 `(150, 180)`、初始航向为 `90° CCW`。若小车部署时
修改该偏移，必须同步更新这里的配置。无人机 FIELD 变换尚未现场确认，界面会持续提示，
不得把该显示用于下发控制。
