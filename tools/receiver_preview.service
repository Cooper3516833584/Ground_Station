[Unit]
Description=Ground-station USB receiver full-screen preview

[Service]
Type=simple
# Template: deploy/install.sh substitutes @APP_DIR@ with the real checkout
# path when it installs this unit.
ExecStart=@APP_DIR@/tools/receiver_preview.sh
Restart=always
RestartSec=1
KillMode=control-group
